# NIPPO UI 修正項目まとめ（2026-08-30）

## 1. 目的と検証範囲

各ユーザーでのログイン、入力操作、画面幅によるレイアウト変化、コメント表示、通知トースト、管理・設定画面の操作導線を検証した結果から、修正すべき項目を重複なく整理したものです。

- 対象画面: 日報、管理、設定、カレンダー設定
- 主な確認ユーザー: `boss1`、`user1`、`tempadmin1` ほかテストデータ登録ユーザー
- 確認画面幅: 1920×1080、1280×800、960×600を中心としたデスクトップ表示
- スマートフォン表示: 今回の対象外
- 本資料は修正方針の整理のみで、アプリ本体の実装変更は含みません。

## 2. 結論

致命的な認証不備、操作不能、入力データ消失は確認されませんでした。ただし、通知が画面外へはみ出す問題と、権限表示が実効権限と一致しない問題は、利用者が操作結果や権限を誤認する可能性があるため優先して修正すべきです。

| 優先度 | 件数 | 概要 |
|---|---:|---|
| P0: 致命的 | 0 | 該当なし |
| P1: 高 | 2 | 長いトーストの画面外はみ出し、派遣社員の管理者権限表示不整合 |
| P2: 中 | 5 | コメント返信表示、トースト位置、カレンダー、管理表、日報フィルター |
| P3: 低 | 2 | 管理表の省略表示、カレンダー凡例の重複 |
| テスト基盤 | 1 | UIテストが途中停止する待機不足 |

## 3. P1: 優先度「高」の修正

### 3.1 長い警告・エラートーストが画面外へはみ出す

- 現象
  - 1280px幅で、日報保存失敗トーストの右端が約111px、カレンダー保存失敗トーストの右端が約140px画面外へ出ました。
  - 右側にある閉じるボタンも画面外へ出る可能性があり、メッセージの確認と手動クローズが困難です。
- 影響
  - 保存失敗など重要な結果を読み切れず、利用者が処理成功と誤認する可能性があります。
- 関連箇所
  - `web/styles.css` の `.status-region`、`.message` 周辺
- 修正方針
  - 通知領域にビューポート内へ収まる最大幅を設定します。
  - 長い日本語メッセージを折り返し可能にします。
  - 閉じるボタンは縮小させず、常に画面内へ残します。
- 完了条件
  - 960px、1280px、1920pxの各幅で、長文の成功・警告・エラートーストが左右とも画面内に収まること。
  - 閉じるボタンが常に表示され、クリックできること。

### 3.2 派遣社員の管理者フラグ表示が実効権限と一致しない

- 現象
  - テストユーザー `tempadmin1` は派遣社員で、実際には管理権限が無効ですが、管理画面の一覧では管理者として有効に見えます。
- 影響
  - 管理者が利用者の権限を誤認し、設定ミスを見逃す可能性があります。
- 関連箇所
  - `web/admin.js` のユーザー権限表示、編集値の正規化処理
- 修正方針
  - データ読込時に、派遣社員では有効にならない管理者フラグを実効権限へ正規化します。
  - または、不正な組み合わせを警告表示し、一覧と編集画面の両方から修正できるようにします。
- 完了条件
  - 一覧、編集画面、実際のアクセス可否が同じ権限状態を示すこと。
  - 不正な組み合わせを保存できないか、保存時に明確な警告が出ること。

## 4. P2: 優先度「中」の修正

### 4.1 上司コメントに対する部下の返信表示が権限によって不統一

- 現象
  - 同じ意味の部下返信が、編集可能な場合はカード状の領域、閲覧のみの場合は区切り線の下のプレーンテキストとして表示されます。
  - 内容ではなく編集権限によって視覚的な意味が変わって見えます。
- 影響
  - 返信と補足情報の区別が分かりにくく、日報カード間で情報構造が統一されません。
- 関連箇所
  - `web/reports.js` の `renderCommentCell`、`renderBossCommentCell`、`renderBossReplyPreview`、`renderDailyCommentCell`、`renderReplySection`
  - `web/reports.css` の `.reply-section`、`.boss-reply-preview`
- 修正方針
  - 閲覧専用・編集可能のどちらでも、返信本文は共通の返信コンポーネントで表示します。
  - 編集可能な場合だけ編集操作を追加し、本文の枠・余白・見出しは変えません。
- 完了条件
  - `boss1` と `user1` の双方で、同じ返信が同じ視覚階層と余白で表示されること。
  - 編集権限の差は、編集ボタンや入力欄の有無だけで判別できること。

### 4.2 トーストの右端位置が画面ごとに揃っていない

- 現象
  - トーストの右余白が、日報では8px、管理では約59px、設定では約112px、カレンダー保存では約59pxでした。
  - 960px幅では管理約116px、設定約112px、カレンダー約46pxとなり、画面幅によって差も変化します。
- 影響
  - 同じ通知コンポーネントが画面ごとに違う基準で配置され、アプリ全体の統一感が損なわれます。
- 関連箇所
  - `web/app.js` の `positionToast()`
  - `web/styles.css` の通知領域
  - `tests/admin/toast.test.cjs` の位置検証
- 修正方針
  - 縦方向は押したボタンの下という現在の画面別アンカーを維持します。
  - 横方向はアンカー位置から切り離し、全画面でビューポート右端から共通の余白に揃えます。既存の日報に合わせて8pxを推奨します。
  - 自動テストも画面別の異なる値ではなく、共通右余白を検証する内容へ変更します。
- 完了条件
  - 日報、管理、設定、カレンダー設定の全トーストが、960px、1280px、1920pxで同じ右余白になること。
  - 各トーストが対応する更新・保存ボタンの下に表示されること。

### 4.3 カレンダーの日付ボタンが中間幅で小さくなりすぎる

- 現象
  - 日付ボタンは960px幅で約31.7px四方、1280px幅で約21.1px四方でした。
  - 1100pxをわずかに超える幅では約17.4×18pxまで小さくなります。
  - 1100px以下で6列から3列へ急に切り替わるため、切替直前が最も操作しづらくなります。
- 影響
  - クリック対象が小さく、日付の選択ミスが起きやすくなります。
- 関連箇所
  - `web/admin.css` のカレンダーグリッドと1100pxブレークポイント
- 修正方針
  - `auto-fit` と `minmax()` を使った流動的な列数、または中間幅用の4列構成へ変更します。
  - 日付ボタンは最低24px、可能なら28～32pxを確保します。
- 完了条件
  - 960～1920pxの間で日付ボタンが24px未満にならないこと。
  - ブレークポイント前後で急激な縮小・拡大が発生しないこと。

### 4.4 管理画面のユーザー表で操作列が横スクロールの先に隠れる

- 現象
  - 960px幅では表の表示領域785pxに対して内容幅が879pxあり、初期表示で右端の操作列約92pxがほぼ見えません。
- 影響
  - 編集・削除が存在することに気づきにくく、対象行を確認した後に横スクロールが必要になります。
- 関連箇所
  - `web/admin.js` のユーザー表カラム定義と操作列
- 修正方針
  - 操作列を右端へ固定し、横スクロール中も常に表示します。
  - 固定列との境界には影や罫線を付け、他のセルとの重なりを明確にします。
- 完了条件
  - 960px幅の初期表示で、横スクロールせず編集・削除操作が見えること。
  - 横スクロール時にも操作列が対象行とずれないこと。

### 4.5 日報の所属・メンバーフィルター名が狭い幅で欠ける

- 現象
  - 1100px以下ではボタン最大幅が118pxに制限され、960px幅で「メーメハーハ係１」「派遣管理者フラグ」などが途中で切れます。
  - 省略内容を確認するツールチップもありません。
- 影響
  - 類似した所属名・ユーザー名を識別しにくく、誤った対象を選ぶ可能性があります。
- 関連箇所
  - `web/reports.css` の1100px以下向けフィルター行スタイル
- 修正方針
  - 横スクロール可能な一列表示は維持しつつ、ボタンを内容に応じた幅にし、上限を約220pxまで広げます。
  - それでも省略が必要な場合のみ、完全な名称をツールチップとアクセシブル名で提供します。
- 完了条件
  - 960px幅で主要な所属名・ユーザー名が原則として全文表示されること。
  - 省略された場合はマウス・キーボード操作の双方で全文を確認できること。

## 5. P3: 優先度「低」の修正

### 5.1 管理画面の表で重要な値が省略され、全文を確認できない

- 現象
  - 960px幅で `tempadmin1`、所属名、権限名などが省略されますが、`title` などの補助表示がありません。
- 影響
  - 利用者や権限の識別に余計な操作が必要で、誤認の原因になります。
- 関連箇所
  - `web/admin.js` のユーザー表カラム定義
- 修正方針
  - セルへ全文を示すネイティブツールチップまたはアクセシブルなツールチップを追加します。
  - IDや権限など識別に重要な列は、必要に応じて自然幅やリサイズ可能な列にします。
- 完了条件
  - 省略された全セルの内容をマウスとキーボードで確認できること。

### 5.2 カレンダー凡例で「休日」が重複表示される

- 現象
  - 「休日」と「休日（計122日）」が別要素として並び、「休日 休日（計122日）」のように見えます。
- 影響
  - 軽微ですが、凡例の意味が冗長で視線が止まります。
- 関連箇所
  - `web/admin.js` のカレンダー凡例・集計表示
- 修正方針
  - 色見本と件数を一つの項目へ統合し、「休日（計122日）」のように表示します。
- 完了条件
  - 「休日」の語が重複せず、色の意味と件数が一読で分かること。

## 6. テスト基盤の修正

### 6.1 `npm run test:ui` が管理者保護状態の確認で途中停止する

- 現象
  - `tools/verify-ui.cjs` の管理者保護状態確認で `Initial protected administrator state mismatch` が発生し、後続テストが実行されません。
  - Chrome上の実表示では `boss1` の削除操作は非表示であり、実装不良よりもDOM反映前の判定である可能性が高い状態です。
- 影響
  - UI回帰テスト全体を完走できず、後半の不具合を自動検出できません。
- 関連箇所
  - `tools/verify-ui.cjs` の初期管理者状態確認
- 修正方針
  - 対象ユーザー行と削除ボタンの状態がDOMへ反映されるまで明示的に待機してから判定します。
  - 固定時間待機ではなく、対象要素または期待状態を条件にした待機にします。
- 完了条件
  - `npm run test:ui` が安定して最後まで完走すること。
  - 連続実行しても同じ箇所で不定期に失敗しないこと。

## 7. 修正不要と確認できた項目

以下は、今回の検証範囲では再修正不要です。

- 管理画面のタブ下線は画面横断ではなく、タブ部分だけに表示されています。
- 管理画面の更新ボタンはタブと同じ行の右端に配置されています。
- カレンダー設定の保存ボタンは更新ボタンの右側にあり、操作グループとして成立しています。
- 設定画面の更新・保存ボタンは最上位のフォルダ設定と同じ行の右端にあり、ボタン間隔も適切です。
- 設定画面のフォーム行間とセクション間隔は、読みやすさと操作密度のバランスが取れています。
- 管理画面のチーム編集におけるマスター・詳細構成は、現状の情報量に対して妥当です。
- 日報画面のツールバーは狭い幅で折り返され、ページ全体の横方向オーバーフローは発生しません。
- 主要ボタンがビューポート外へ完全に出る問題は、長文トースト内の閉じるボタンを除いて確認されませんでした。
- データ量が少ない設定・管理画面の下部に余白が残ることは、作業領域として許容でき、欠陥とは判断しません。

## 8. 推奨する実施順序

1. UIテストの待機不足を直し、以降の修正を回帰テストできる状態にする。
2. トーストの画面内収容と右端統一を同じ通知コンポーネント改修として実施する。
3. 派遣社員の管理者権限表示を実効権限と一致させる。
4. 上司コメントへの部下返信表示を共通コンポーネントへ統一する。
5. カレンダーのレスポンシブ列数と日付ボタン最小サイズを調整する。
6. 管理画面の操作列を固定し、省略セルの全文確認手段を追加する。
7. 日報フィルターボタンの幅とツールチップを調整する。
8. カレンダー凡例の重複を整理する。

## 9. 修正後の回帰確認項目

- 画面幅: 1920×1080、1280×800、1110px前後、960×600
- テーマ: ライト、ダーク
- 文字サイズ: 標準、大
- ユーザー種別: 上司、一般社員、派遣社員、管理権限を持つユーザー、権限値が不正なテストユーザー
- 操作: 初期表示、入力、保存、更新、エラー発生、トースト手動クローズ、横スクロール、キーボード操作
- コメント: 上司コメントなし・あり、部下返信なし・あり、返信編集可能・閲覧のみ
- 自動テスト: トーストの共通右余白、画面内収容、実効権限表示、操作列表示、カレンダー最小サイズ、フィルター名の確認手段を追加する

