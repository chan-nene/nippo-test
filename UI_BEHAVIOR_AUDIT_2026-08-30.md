# NIPPO UI・挙動監査（2026-08-30）

## 結論

- 致命的: 0件
- 要修正: 3件
- 軽微: 0件
- テスト基盤: 1件

テストデータの7ユーザーで、権限別初期表示、日報・コメント・返信の入力と保存、管理・カレンダー・設定、未保存確認、正常系・異常系通知、ライト／ダーク、5段階の文字サイズ、1920×1080・1280×800・960×600を確認した。権限漏れ、入力消失、主要操作不能などの致命的な問題は確認されなかった。

## 要修正

### 1. 長いエラー／警告トーストが画面右端からはみ出す

- Tell: 長いトーストが通知領域の幅を超えたときに折り返されず、画面右側へ伸びる。文末と閉じるボタンが画面外へ出る。
- Where: 日報の保存失敗、カレンダー設定の保存失敗など、右上アクションを基準に表示する長いエラートースト。
- Severity: 要修正
- 再現:
  1. `http://127.0.0.1:4173/?user=user1&fail=reports-save` を開く。
  2. 日報を編集して保存する。
  3. 表示されたエラートーストを確認する。
- 計測結果:
  - 日報保存失敗（1280幅）: トースト右端 `1391.09px`、画面幅 `1280px`、約 `111px` はみ出し。
  - カレンダー保存失敗（1280幅）: トースト右端 `1420.09px`、約 `140px` はみ出し。
- 影響: エラー内容を最後まで読めず、閉じるボタンも操作できない。狭いデスクトップ幅ほど悪化する。
- Fix: `.message` に通知領域内で収まる `max-width: 100%` と折り返し可能なレイアウトを設定し、右寄せ時に子要素が右方向へ拡張しないようにする。閉じるボタンは縮小不可にする。
- 証拠: `artifacts/ui-audit-2026-08-30/reports-save-failure-retains-input.png`

### 2. 派遣社員の無効な管理者フラグが、有効な「管理者」として表示される

- Tell: `employment_type=temporary,is_admin=1` のユーザーが、管理一覧では緑色の「管理者」と表示される。一方、ログイン時は正しく管理画面へ入れず、実効権限と表示が矛盾する。
- Where: 管理画面 → ユーザー一覧、派遣管理者フラグ行。
- Severity: 要修正
- 再現:
  1. `tempadmin1` でログインし、管理メニューがないことを確認する。
  2. `boss1` で管理画面を開き、`tempadmin1` の権限列を確認する。
  3. 編集ダイアログでは「管理者」が選択済みかつ権限ラジオが無効になっている。
- 影響: CSVの外部編集や旧データで不整合が入った場合、管理者が実効権限を誤認する。権限エスカレーションは確認されていない。
- Fix: 派遣社員の `is_admin=1` は読込時に一般として正規化するか、「無効な管理者フラグ」の警告を表示し、編集画面から修正できる導線を用意する。
- 証拠: `artifacts/ui-audit-2026-08-30/admin-user-permission-boundary.png`

### 3. 同じ「部下からの返信」が、上司コメント列ごとにカード表示とプレーン表示へ分かれる

- Tell: 同じ種類の部下返信が、灰色背景・返信アイコン・角丸を持つカードと、破線の区切り線下に置かれるプレーン文字の2種類で表示される。同じ行・同じ画面内でも混在する。
- Where: 日報画面の上司コメント列。`boss1` で2026/08/24〜2026/08/25を表示した場合、主幹列への返信はカード、係長1列への返信はプレーン表示になる。`user1` 本人画面の返信もプレーン表示。
- Severity: 要修正
- 再現:
  1. `boss1` でログインし、期間を2026/08/24〜2026/08/25にする。
  2. `user1` の係長1列と主幹列を比較する。
  3. 主幹列の `了解です` / `oook` はカード、係長1列の `ok` は破線下のプレーン文字になる。
  4. `user1` で同じ期間を表示すると、本人が入力した返信はプレーン文字になる。
- 実装上の原因: `canEditComment=true` のコメント列は `renderBossCommentCell()` → `renderBossReplyPreview()` を通り、`.boss-reply-preview` のカードになる。それ以外は `renderDailyCommentCell()` → `renderReplySection()` → `.reply-content` を通り、破線下のプレーン表示になる。返信内容ではなく、上司コメントの編集権限によって表示部品が変わっている。
- 計測結果:
  - カード: 背景 `rgb(247, 247, 248)`、角丸 `5px`、余白 `6px 8px`、左アクセント罫線あり、高さ約 `35.6px`。
  - プレーン: 背景透明、枠なし、余白 `11px 12px`、親要素上部に `1px dashed`、高さ `49px`。
- 影響: 部下返信という同じ意味の情報に異なる視覚表現が割り当てられ、カードが選択状態・重要情報・別種データのように見える。行高も不均一になる。
- Fix: 返信の読取表示を1つの共通コンポーネントへ統一する。編集可能な本人返信だけ編集操作を付加し、読取状態は上司・部下・コメント編集権限にかかわらず同じ背景、区切り、アイコン、余白にする。
- 証拠:
  - `artifacts/ui-audit-2026-08-30/reply-boss-view-mixed.png`
  - `artifacts/ui-audit-2026-08-30/reply-boss-other-supervisor-plain.png`
  - `artifacts/ui-audit-2026-08-30/reply-user-view-plain.png`

## テスト基盤

### `npm run test:ui` が管理者保護確認で停止する

- `tools/verify-ui.cjs:2024` の `Initial protected administrator state mismatch` で停止した。
- Chrome実画面では `boss1` の削除ボタンは非表示で、保護そのものは機能している。
- 自動テストは管理ワークスペースの表示だけを待ち、ユーザー行の描画完了を待たずに判定しているため、行が未取得の状態を `hidden:false` と評価している可能性が高い。
- Fix: `u001` のユーザー行と削除ボタンがDOMへ作成されるまで待ってから、`hidden` と `aria-hidden` を検証する。
- この停止により、同スクリプト後半の自動検証は完走していない。該当する主要画面・文字サイズ・画面幅はChrome実画面で補完した。

## ユーザー別結果

| ユーザー | 確認した役割・結果 |
|---|---|
| `boss1` | 正社員管理者。管理メニュー表示、課全体の閲覧・コメント、本人日報入力。本人削除は非表示。 |
| `boss2` | 本人日報入力不可、係コメント担当。管理メニューなし、他ユーザー日報は編集不可。 |
| `assist1` | 本人入力あり、係コメント担当。日次コメントの入力・保存に成功。 |
| `user1` | 正社員一般。本人日報の入力、複数行、日本語、返信、保存に成功。同係の閲覧は可能だが他人の日報は編集不可。 |
| `user2` | 派遣社員。本人だけを表示し、他ユーザー・管理画面へアクセス不可。 |
| `director1` | 部長。本人日報なし。週の最終稼働日である金曜日だけに週次コメント操作が表示され、保存に成功。 |
| `tempadmin1` | 派遣社員かつ管理者フラグあり。実効権限は一般ユーザー相当で、本人表示だけ、管理メニューなし。 |

## 操作結果

### 日報

- 日本語、全角・半角、数字、複数行を入力できる。
- Tabで業務名から業務詳細へ移動する。
- 保存成功後は未保存状態が解除され、成功トーストは保存ボタン直下へ表示される。
- 更新後もモックのセッション内保存内容を保持する。
- 保存失敗時は入力と未保存状態を保持し、再保存できる。
- 一般ユーザーの返信、係コメント担当の日次コメント、部長の週次コメントを保存できる。
- 未保存状態で画面移動すると確認ダイアログが開き、初期フォーカスは「このまま編集を続ける」。Escで編集画面へ戻り、「破棄して画面を移動」で遷移する。
- 960幅では本文テーブルのみ横スクロールし、画面全体の横スクロールは発生しない。

### 管理・カレンダー

- タブ下線はタブ領域だけ（1920幅でタブ幅約263px、パネル幅約1824px）。
- 更新ボタンはタブと同じ行の右端。
- カレンダーの保存ボタンは更新ボタンの右側で同じ高さ。
- 更新・保存成功トーストは各ボタン直下へ表示される。
- カレンダー保存失敗時は変更と保存可能状態を保持する。
- ユーザー追加の必須・重複入力では対象入力へフォーカスし、重複エラーを表示する。

### 設定

- 最上位フォルダ入力、更新、保存は同じ行。更新の右側に保存が配置される。
- 更新トーストは更新ボタン直下へ表示される。
- 保存失敗は設定領域内の `role=alert` で表示され、入力値を保持する。
- 960幅・極大文字でも各入力と右上ボタンは画面内に収まり、画面全体の横スクロールは発生しない。

### 表示・アクセシビリティ

- 1920×1080、1280×800、960×600を確認。スマートフォン幅は対象外。
- ライト／ダークの画面遷移保持を確認。
- 極小・小・中・大・極大の全5段階を960幅の日報・設定で確認。右上アクションは画面内に収まる。
- 成功通知は `role=status`、保存失敗通知は `role=alert`。
- 入力セル、コメント、返信、フィルター、カレンダー日付、ダイアログ操作に判別可能なアクセシブル名がある。

## 証拠画像

- `artifacts/ui-audit-2026-08-30/admin-user-permission-boundary.png`
- `artifacts/ui-audit-2026-08-30/settings-960-dark-xlarge.png`
- `artifacts/ui-audit-2026-08-30/reports-960-dark-xlarge.png`
- `artifacts/ui-audit-2026-08-30/reports-save-failure-retains-input.png`
- `artifacts/ui-audit-2026-08-30/reply-boss-view-mixed.png`
- `artifacts/ui-audit-2026-08-30/reply-boss-other-supervisor-plain.png`
- `artifacts/ui-audit-2026-08-30/reply-user-view-plain.png`

## 実行したテスト

- `python -m unittest discover -s tests -v`: 108件成功
- `npm run test:unit`: 30件成功
- `npm run test:ui`: 管理者保護確認で停止（上記テスト基盤項目）
- Chrome実画面: 7ユーザー、3画面幅、ライト／ダーク、5文字サイズ、正常系・保存／読込失敗系
