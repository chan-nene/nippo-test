# JavaScriptライブラリ導入計画

## 方針

既存の画面仕様と`file://`実行を維持しながら、ライブラリの得意な表示部分だけを置き換える。状態管理、保存API、未保存確認、権限判定は既存のアプリ側コードを正とする。

| 対象 | 方針 | 備考 |
| --- | --- | --- |
| 日報の日付範囲入力 | Flatpickrを導入 | `range`モード。既存の範囲表示・保存値を維持 |
| 設定の集計開始日 | Flatpickrを導入 | `single`モード。日報側と同じ操作感にする |
| 管理画面のユーザー一覧 | Tabulatorを導入 | 検索・行選択・編集／削除アクションを既存処理へ接続 |
| 管理画面の課・係・組織階層 | 現行実装を維持 | 階層、Sortable、モーダルとの結合が強いため |
| 管理画面の休日設定カレンダー | 現行実装を維持 | 4月〜翌年3月、休日切替、キーボード操作を維持 |

## 実装段階

### 1. 共通準備

- 採用バージョンを固定する
- 必要なminified JS、CSS、ライセンス文を`web/vendor/`へ配置する
- CDN、動的import、ES module依存は使用しない
- `index.html`の既存script順とCSPを維持する
- `node --check`、`file://`での初期表示、コンソールエラーを確認する

### 2. Flatpickr（日報・設定の日付入力）

- 先に設定画面の単一日付入力へ適用する
- 操作確認後、日報画面の範囲入力へ適用する
- 表示形式と内部値（`YYYY/MM/DD`／`YYYY-MM-DD`）を既存仕様に合わせる
- 入力欄クリック時の文字列選択を防ぎ、カレンダーアイコンを含む操作を確認する
- 文字サイズ、ライト／ダーク、キーボード操作、範囲選択を確認する

### 3. Tabulator（ユーザー管理表）

- 既存のユーザー管理表をTabulatorへ置き換える
- 列、検索、行選択、編集アイコン、削除アイコンを再現する
- 編集・追加は既存または簡易モーダルへ接続する
- 削除不可ユーザーの削除操作は既存ルールどおり非表示または無効化する
- `admin.js`のdraft、dirty、保存、ロールバック、権限判定は変更しない
- 課・係の階層表、Sortable、カレンダー設定は置き換えない


## 受入条件

- `file://`およびPyInstaller one-file exeで初期表示できる
- CSP違反、外部CDN通信、コンソールエラーがない
- Flatpickrの単一／範囲入力が既存の値形式で動作する
- Tabulatorの検索、行選択、編集、削除、モーダル起動が動作する
- 既存の保存、未保存警告、ロールバック、権限判定に回帰がない
- 組織階層、Sortable、休日設定カレンダーの表示と操作に回帰がない
- ライト／ダークテーマ、レスポンシブ表示、文字サイズ変更で破綻しない

## 非対象

- ES module化やビルドシステム導入
- ライブラリによる保存APIやデータモデルの変更
