# NIPPO UI/UX 改善検証

## 目的

本番 UI を直接変更せず、`web/uiux-playground.html` で改善案を比較・検証し、
効果が確認できたものだけを本番へ段階的に反映する。

対象は日報入力、上司コメント、設定画面で共通して使われる操作・状態・
アクセシビリティである。業務ロジック、データ取得、保存形式は対象外とする。

## 現状

- `npm run test:ui` は成功しており、既存の機能・レスポンシブ検証は通過している。
- 情報密度、インライン編集、未保存表示、ダークモード、文字サイズ切替は良好。
- 一方で、キーボードフォーカス、空セルの編集発見性、操作領域、
  狭い画面での上司コメント操作に改善余地がある。

## 検証の進め方

1. `web/uiux-playground.html` で「現状相当」と「改善案」を切り替える。
2. マウス、キーボード、タッチ相当、標準文字、特大文字で確認する。
3. 下記の受け入れ条件を満たした項目だけを本番へ反映する。
4. 反映は課題単位で行い、既存の `npm run test:ui` に回帰テストを追加する。

---

## P0-1: フォーカスリングのコントラスト不足

### 現象

グローバルのフォーカスリングがアクセント色と透明色の混色になっており、
白背景では約 1.85:1、期間移動ボタンでは約 2.02:1 となる。
非テキスト UI に必要な 3:1 を下回る。

### 根拠

- `web/styles.css:282`
- `web/reports.css:219`

### リスク

キーボード利用者が現在位置を見失い、保存、期間変更、画面移動を安全に行えない。

### 改善仮説

背景から独立した `--focus-ring` トークンを設け、2px 以上の実線リングを使う。
ライト・ダーク・各アクセント色で 3:1 以上を満たす。

### 受け入れ条件

- Tab 移動中、すべての操作要素でフォーカス位置が即座に分かる。
- リングは白、淡色、濃色背景のすべてで 3:1 以上。
- フォーカスリングの出現をアニメーションしない。
- マウスクリック時に不要なリングを常時表示しない。

### Playground

- 「フォーカス: 現状 / 改善」を切り替えて比較する。
- Tab キーでサイドバー、期間、セル、保存ボタンを順番に移動する。

---

## P0-2: 空の編集セルにアクセシブルネームがない

### 現象

空の編集セルは `role="button"` と `tabindex="0"` を持つが、
テキストと `aria-label` がないため、支援技術には「名前のないボタン」と伝わる。

### 根拠

- `web/reports.js:1374`
- `web/reports.js:1386`
- `web/reports.css:675`

### リスク

スクリーンリーダー利用者が、どの日付・どの列を編集する操作なのか判断できない。

### 改善仮説

空・入力済みにかかわらず、日付、列名、状態を組み合わせた名前を付与する。

例:

```text
6月4日の業務詳細、未入力。Enterキーで編集
6月3日の業務名、フロントエンド作成。Enterキーで編集
```

### 受け入れ条件

- 空セルを含むすべての編集セルに一意で具体的な名前がある。
- Enter と Space で編集を開始できる。
- Escape で編集を終了でき、元のセルへフォーカスが戻る。
- スクリーンリーダーなしでも同じ操作方法を理解できる。

### Playground

- 「空セル表示: 空白 / ヒントあり」を切り替える。
- アクセシブルネームを画面上の検証パネルにも表示する。

---

## P1-1: 空セルの編集可能性がホバー依存

### 現象

空セルは通常時に完全な空白で、マウスを重ねた後にだけ枠と背景が現れる。
タッチ操作ではホバーがなく、キーボード利用者にも編集可能性が事前に伝わらない。

### 根拠

- `web/reports.css:646`
- `web/reports.css:655`
- `web/reports.css:664`
- `web/reports.css:675`

### 改善仮説

空の編集セルには低コントラストの「入力」または「未入力」ヒントを常時表示し、
ホバー・フォーカス時に編集意図を強める。

### 受け入れ条件

- 静止状態だけで編集可能なセルを判別できる。
- ヒントは実データと誤認されない。
- 休日・読み取り専用セルは編集可能に見えない。
- 行の情報密度を大きく損なわない。

### Playground

- 空白、テキストヒント、アイコン付きヒントを比較する。

---

## P1-2: 押下中のインタラクション状態が不足

### 現象

hover、選択、disabled はあるが、主要ボタンにポインター押下中の
`:active` フィードバックがない。

### 根拠

- `web/reports.css:109`
- `web/settings.css:155`
- `web/styles.css:555`

### 改善仮説

色または 1px の移動だけを使い、押下を即時に伝える。
複数の効果を同時に使わず、フォーカスリングは維持する。

### 受け入れ条件

- 保存、期間移動、プリセット、サイドバーで押下が認識できる。
- `:active` 中もラベルのコントラストを維持する。
- transform を使う場合は 1px 以内。
- disabled 要素には押下表現を出さない。

### Playground

- ボタンの default、hover、focus、active、disabled を並べて確認する。

---

## P1-3: 主要操作のクリック領域が小さい

### 現象

ヘッダーアイコンは 30px、サイドバー項目は 36px、
標準時の入力・コントロールは 32〜36px である。

### 根拠

- `web/styles.css:61`
- `web/styles.css:394`
- `web/styles.css:520`
- `web/reports.css:78`

### 改善仮説

見た目の密度を保ちながら、疑似要素、padding、行高により
主要操作の実効クリック領域を 44px 以上にする。

### 受け入れ条件

- 主要なボタン、アイコン、入力の操作領域が 44x44px 以上。
- 表のデータ密度は維持する。
- 標準・大・特大文字で隣接要素と重ならない。
- 960px 幅でツールバーが破綻しない。

### Playground

- 「操作領域: 36px / 44px」を切り替える。
- 操作領域の外形を可視化できる。

---

## P1-4: 上司画面の横方向操作が発見しにくい

### 現象

上司画面の表は最小幅 1240px で、960px・特大文字では
上司コメント列が初期表示範囲外になる。メンバー一覧も横スクロールになるが、
続きがあることを示す視覚的な手掛かりが弱い。

### 根拠

- `web/reports.css:450`
- `web/reports.css:939`

### リスク

上司画面の主要タスクであるコメント追加に到達しにくい。

### 改善仮説

- メンバー一覧の左右端にフェードまたは移動ボタンを表示する。
- 選択したメンバーを自動的に表示範囲へ移動する。
- コメント操作列を右端固定、または選択行のインスペクターへ分離する。

### 受け入れ条件

- 960px・特大文字でも、コメント操作への到達方法が一目で分かる。
- メンバー一覧の続きがあることを視認できる。
- 選択中メンバーが必ず表示範囲内にある。
- 表の横スクロール中も日付・氏名・コメント操作の文脈を維持する。

### Playground

- 「コメント操作: 表内 / 右固定インスペクター」を比較する。
- メンバー一覧の端部フェードとスクロールボタンを試す。

---

## P1-5: サイドバーの現在画面が支援技術へ伝わらない

### 現象

現在画面は `.is-active` クラスで視覚的に示されるが、
`aria-current` または `aria-pressed` が更新されない。

### 根拠

- `web/app.js:541`
- `web/app.js:551`

### 改善仮説

画面移動を行うボタンに `aria-current="page"` を付け、
非選択ボタンからは属性を外す。

### 受け入れ条件

- 選択画面が視覚とアクセシビリティツリーの両方で一致する。
- 一度に `aria-current="page"` を持つ要素は1つだけ。
- 折りたたみ状態でも各ボタンの名前と現在状態が分かる。

### Playground

- サイドバー選択時の `aria-current` 値を検証パネルへ表示する。

---

## P1-6: レイアウトプロパティのアニメーションと動きの抑制不足

### 現象

サイドバー開閉で `width`、`flex-basis`、`max-width` をアニメーションしている。
全体に適用される十分な `prefers-reduced-motion` 対応がない。

### 根拠

- `web/styles.css:487`
- `web/styles.css:498`
- `web/styles.css:598`
- `web/admin.css:853`

### 改善仮説

#### 候補A: レイアウトは即時切り替え（Hallmark原則に忠実）

レイアウトサイズは即時に切り替え、ラベルのみ opacity で切り替える。
`width` や `flex-basis` のフレームごとの再計算を避けられるため、
表示内容や実行環境が変わっても安定しやすい。

#### 候補B: 短時間の開閉アニメーションを残す（プロダクト判断）

サイドバーの開閉は状態変化の方向と因果関係を伝え、
Workbench型UIの操作感を高めるため、アニメーションを残す案も候補とする。
Hallmarkはモーション自体を禁止しておらず、意図を説明できる
2〜3個の小さなマイクロインタラクションをWorkbench型UIでは推奨している。

一方、Hallmarkの一般原則では、毎フレーム再レイアウトが発生する
`width`、`flex-basis`、`max-width` のアニメーションは避け、
`transform` と `opacity` を優先する。この案はその原則に対する
意図的な例外として扱い、次の条件を満たす場合に採用する。

- 開閉はユーザー操作を起点とし、頻繁に自動再生されない。
- 時間は 150〜200ms 程度に収め、ブラウザー既定の `ease` ではなく
  状態切り替え用の `cubic-bezier(0.65, 0, 0.35, 1)` を名前付きトークンで使う。
- 対象プロパティを明示し、`transition: all` は使わない。
- 開閉中も表のスクロール位置、入力フォーカス、キャレット、
  sticky要素の位置関係が維持される。
- 想定する低性能端末とWebView2で、目立つカクつきや操作遅延がない。
- `prefers-reduced-motion: reduce` では空間移動を停止し、
  必要なら150ms以下の opacity 変化だけにする。

短時間の表の再配置が多少見えても、上記を満たし、
実利用で操作や読解を阻害しないなら候補Bを採用可能とする。
最終判断はHallmarkの一般則だけで自動決定せず、Playgroundの比較と
実機確認に基づく。

### 受け入れ条件

- 候補Aと候補Bを比較し、採用理由を記録できる。
- 候補Bでは、サイドバー開閉が150〜200ms程度で完了し、
  状態変化の方向が自然に分かる。
- サイドバー開閉時に表や入力欄の再配置が不自然に揺れず、
  スクロール位置、入力フォーカス、キャレットが維持される。
- 想定する低性能端末とWebView2で目立つフレーム落ちがない。
- `prefers-reduced-motion: reduce` で空間移動が停止する。
- フォーカスリングは常に即時表示される。

### Playground

- 「モーション: レイアウト開閉 / 安全な切り替え / 停止」を手動で切り替える。
- 候補Bでは名前付き `--ease-in-out` を使い、
  ブラウザー既定の `ease` との差も確認できるようにする。
- ブラウザーの `prefers-reduced-motion` 設定でも自動的に抑制される。

---

## P2-1: カラートークン外の直接指定

### 現象

土日色、バッジ色、テーマ見本などに直接の HEX 値が残り、
テーマ変更時にも同じ色が使われる。

### 根拠

- `web/reports.css:155`
- `web/reports.css:607`
- `web/settings.css:123`

### 改善仮説

`--text-saturday`、`--text-sunday`、`--badge-danger`、
`--focus-ring` など、意味ベースのトークンへ集約する。

### 受け入れ条件

- コンポーネント規則内に HEX、rgb、hsl の直接指定を残さない。
- グリーン、ブルー、オレンジ、ダークで同じ意味の色が一貫する。
- 曜日色とエラー色を混同しない。

### Playground

- グリーン、ブルー、オレンジ、ライト、ダークを組み合わせて確認する。
- 使用中の意味トークンを検証パネルへ表示する。

---

## 本番反映の順序

1. P0-1 フォーカスリング
2. P0-2 編集セルのアクセシブルネーム
3. P1-1 空セルの編集発見性
4. P1-5 サイドバーの現在画面
5. P1-3 操作領域
6. P1-2 押下状態
7. P1-4 上司画面の横方向操作
8. P1-6 モーション
9. P2-1 カラートークン

## 完了条件

- Playground で各改善を個別に有効・無効にできる。
- ライト・ダーク、3アクセント、標準・特大文字で確認できる。
- キーボードだけで主要なデモ操作を完了できる。
- 960px と 1280px でレイアウトを比較できる。
- 本番ファイルを変更していない。
- 採用する改善ごとに本番向けの小さな変更単位を決められる。
