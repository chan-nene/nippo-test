# NIPPO 最終挙動・メッセージ仕様

最終更新日: 2026-08-30

## 1. この文書の目的

今回の改修後に確定した、画面遷移、データの再読み込み、保存、入力エラー、競合、トーストおよび管理操作の最終仕様を記録する。

画面に表示する文言は、原則としてこの文書に記載した文字列を使用する。バックエンドが個別のアクセス拒否理由を返した場合など、明示した箇所ではバックエンドの文言を優先する。

## 2. メッセージの表示先

メッセージは内容と操作場所に応じて、次の3種類に分ける。

| 表示先 | 用途 |
| --- | --- |
| トースト | 操作結果、手動再読み込みの結果、画面全体に関係する競合など |
| 画面内メッセージ | 初回表示・画面遷移時の読み込み失敗、データの一部読み込み失敗など |
| インラインエラー | モーダルや設定フォーム内の入力エラー、モーダルから実行した保存の失敗 |

モーダル内で利用者が修正できるエラーは、背後の画面にトーストを出さず、モーダル内に表示する。競合はモーダルだけの問題ではなく管理画面全体のデータが古い状態であるため、例外として管理画面のトーストで通知する。

## 3. トースト共通仕様

### 3.1 表示方式

- トーストは画面ごとに管理し、発生元の画面でのみ表示する。
- 画面を移動した場合、移動元のトーストは非表示になるが、その画面に戻ると残り時間を引き継いで再表示する。
- 同一画面内で「種類」と「文言」が同じトーストは重複して積まず、既存のトーストの表示時間をリセットする。
- 同時表示は最大3件とし、4件目以降は発生順に待機する。表示中のものが閉じると、待機中の先頭を表示する。
- トーストには状態を表す独立したマークを付けない。
- 各トーストの右端に手動で閉じる `×` ボタンを表示する。
- 成功・情報は4秒、警告・エラーは7秒で自動的に閉じる。
- 競合や保存後の表示更新失敗など、利用者の追加操作が必要な重要メッセージは自動で閉じない。
- マウスを重ねている間、トースト内にフォーカスがある間、確認ダイアログまたは編集モーダルを表示している間、別画面を表示している間は自動消去時間を停止する。
- トーストは各画面の上部ツールバー直下・右寄せに表示し、日報画面の「最新データを取得」「変更を保存」ボタンとは重ねない。
- 成功・情報は `role="status"`、警告・エラーは `role="alert"` として読み上げる。

### 3.2 表示しない通知

- 読み込み中・保存中を表すトーストは表示しない。
- 保存中にボタンのラベルは変更しない。二重送信防止のため保存ボタンだけを無効にする。
- 保存件数、失敗件数、成功・失敗の内訳は表示しない。
- 管理画面の並び替え保存成功時はトーストを表示しない。
- 表示設定の小さな変更にはトーストを表示しない。

## 4. 画面遷移と再読み込み

### 4.1 ナビゲーション

- 日報、管理、設定の別画面へ移動するときは、移動先で必要なデータを毎回最新取得してから表示する。
- 現在表示している画面と同じナビゲーションボタンを押した場合は何もしない。再読み込みもしない。
- すべての画面に再読み込み操作を常設する。最新取得を行いたい場合は、現在画面の再読み込み操作を使用する。
- 管理画面は管理者だけが表示できる。日報画面へのアクセスが拒否されても、設定画面は利用可能な状態を維持する。

### 4.2 未保存変更がある場合

別画面への移動または現在画面の再読み込みを行う前に、現在画面に未保存変更があれば確認ダイアログを表示する。

画面移動時の表示は次のとおり。

- タイトル: `未保存の変更があります`
- 説明: `画面を移動すると、保存していない変更は失われます。`
- 実行: `破棄して画面を移動`
- キャンセル: `このまま編集を続ける`

再読み込み時は、説明と実行ボタンの「画面を移動」部分を各画面の再読み込み操作名に置き換える。

- 実行した場合: 現在画面の下書きを破棄し、移動または再読み込みを続行する。
- キャンセルした場合: データや選択状態を変更せず、現在画面に留まる。
- アプリを閉じるときに未保存変更がある場合も確認し、`破棄して終了` または `終了しない` を選択する。

### 4.3 読み込み失敗時の共通方針

- 初回表示または別画面への遷移で必要なデータを読み込めなかった場合は、中途半端なデータを表示しない。画面内容を隠し、画面内にエラーを表示する。
- 手動再読み込みに失敗し、すでに完全なデータを表示している場合は、その表示を維持してトーストで失敗を通知する。
- 一部データだけを読み込めなかった場合は、利用可能な画面を表示したうえで、画面内に再読み込みを促す警告を表示する。
- 読み込み失敗を例外として握り潰さず、詳細はアプリケーションログにも記録する。

## 5. 日報画面

### 5.1 日付範囲

- 日付範囲はFlatpickrの `range` モードを使用し、1個のテキスト入力として表示する。
- カレンダーアイコンはアプリ側で表示するSVGであり、ネイティブの `input type="date"` が重なっているものではない。
- 入力欄の外幅は日付変更で動かない。文字サイズを変更しても日付文字列とアイコンが重ならないよう、テキスト領域と固定アイコン領域を分ける。
- 設定画面の日付入力も同じ考え方で、テキストとカレンダーアイコンの配置を行う。

### 5.2 最新データの取得

- 日報画面へ別画面から移動したときは、日報表の構成に必要な管理データを含めて最新取得する。
- `最新データを取得` を押したときに未保存変更があれば、破棄確認を表示する。
- 手動取得に成功した場合: `最新データを取得しました。`
- 手動取得に失敗し、現在の表示を維持できる場合: `最新データを取得できませんでした。もう一度お試しください。`
- 初回・画面遷移時の取得失敗: `日報データを読み込めませんでした。「再読み込み」を押してください。`
- 一部取得失敗: `一部のデータを読み込めませんでした。もう一度「最新データを取得」を押してください。`
- 全データの取得に失敗した場合は、日報表を空にして画面内エラーだけを表示する。

### 5.3 変更の保存

- 保存中は `変更を保存` ボタンを無効にし、ラベルは変更しない。
- 保存成功: `変更を保存しました。`
- 一部だけ保存成功: `一部の変更を保存できませんでした。もう一度保存してください。`
- 全件保存失敗: `変更を保存できませんでした。もう一度保存してください。`
- ディスクへの保存は成功したが、キャッシュまたは画面への反映に失敗した場合: `変更は保存されましたが、表示を更新できませんでした。「最新データを取得」を押してください。`
- 一部保存の場合、保存できた変更だけを画面と下書きへ反映し、保存できなかった変更は未保存状態のまま残す。
- 保存後に日報全体を自動再読み込みせず、保存できた行を現在の画面へ反映する。
- 保存対象がない場合は何も表示しない。
- キャッシュまたは画面反映の警告は、利用者が確認して最新取得する必要があるため自動で閉じない。

## 6. 設定画面

### 6.1 読み込み

- 別画面から設定画面へ移動したときは、設定を最新取得する。
- 手動再読み込み成功: `設定を再読み込みしました。`
- 手動再読み込み失敗で現在表示を維持できる場合: `設定を読み込めませんでした。もう一度お試しください。`
- 初回・画面遷移時の読み込み失敗: `設定を読み込めませんでした。「再読み込み」を押してください。`

### 6.2 入力エラーと保存

- 保存ボタン押下時に入力内容を検証する。
- 入力エラーは対象入力欄の直下へ赤字で表示し、最初のエラー項目へフォーカスする。
- 項目を編集したときは、その項目のエラーだけを解除する。
- 必須フォルダの文言は次のとおり。
  - `ユーザー日報フォルダを入力してください。`
  - `上司コメントフォルダを入力してください。`
  - `管理フォルダを入力してください。`
- 項目に紐づかない保存失敗は、保存操作付近に `設定を保存できませんでした。もう一度保存してください。` と表示する。
- 保存成功: `設定を保存しました。`
- 保存成功後も設定画面に留まる。

### 6.3 未確認コメントの開始日

- 未確認コメントの開始日を空のまま保存しても警告は表示しない。
- バックエンドで未設定または不正な日付と判定した場合は、`2026-04-01` を開始日として扱う。
- カレンダー設定で作成できる年度は2026年度以降とする。
- 年度下限とデフォルト開始日は `app/calendar_policy.py` の `CALENDAR_MIN_FISCAL_YEAR` を基準に一元管理する。

## 7. 管理画面

### 7.1 読み込み

- 別画面から管理画面へ移動したときは、ユーザー、組織、コメント対象、カレンダーなど管理画面に必要なデータを最新取得する。
- 手動再読み込み成功: `最新データを読み込みました。`
- 手動再読み込み失敗で現在表示を維持できる場合: `最新データを読み込めませんでした。もう一度お試しください。`
- 初回・画面遷移時の読み込み失敗: `管理データを読み込めませんでした。「再読み込み」を押してください。`
- 一部読み込み失敗: `一部の管理データを読み込めませんでした。もう一度「最新データを再読み込み」を押してください。`
- 全データを読み込めない場合は管理内容を表示しない。

### 7.2 ユーザー管理表

- 表はTabulatorで表示する。
- 表の横罫線は実際に表示している行にだけ付け、データのない領域まで罫線を引かない。
- 画面幅が狭い場合は表内に横スクロールを表示し、右端の編集操作まで移動できる。
- 検索欄は全列を対象にする。タグ表示している列も、元の文字列を検索対象に含める。
- 雇用区分は `正社員` または `派遣社員` をグレー系タグで表示する。
- 権限は管理者だけを、日報表のYouタグと同系統の緑色タグで表示する。一般ユーザーは空白にする。
- 日報入力は `要` だけを黄色系タグで表示する。`不要` は空白にする。

### 7.3 ユーザー・課・係の作成と編集

- ユーザーと課・係の編集はモーダルで行う。
- 作成・更新ボタンを押した時点で検証し、エラーを対象項目の直下へ赤字表示する。
- 最初のエラー項目へフォーカスする。
- 入力を変更した項目について、その項目のエラーだけを解除する。
- モーダル内の保存失敗はモーダル内に `データを保存できませんでした。もう一度更新してください。` と表示し、入力内容を保持する。
- モーダルは保存成功時、または利用者が `×` を押した場合に閉じる。Escキーや背景クリックでは閉じない。利用者が `×` で閉じた場合は、モーダルを開く前の内容へ戻す。

ユーザーの主な入力規則は次のとおり。

- 社員番号、氏名、雇用区分、所属区分、日報入力は必須とする。
- 所属組織そのものは必須にせず、`所属登録なし` を選択できる。
- 社員番号重複: `この社員番号は既に登録されています。`
- 必須テキスト項目: `{項目名}を入力してください。`
- 必須選択項目: `{項目名}を選択してください。`

課・係の主な入力規則は次のとおり。

- 課名未入力: `課名を入力してください。`
- 係名未入力: `係名を入力してください。`
- 係の親課未選択: `親課を選択してください。`
- 課名重複: `同じ名前の課が既に登録されています。`
- 同一課内の係名重複: `この課には同じ名前の係が既に登録されています。`

保存成功時のトーストは次のとおり。

- `ユーザーを追加しました。`
- `ユーザー情報を更新しました。`
- `課を追加しました。`
- `課を更新しました。`
- `係を追加しました。`
- `係を更新しました。`

正社員の管理者を0人にする変更は許可しない。文言は `正社員の管理者を1人以上残す必要があるため、{操作}できません。` とする。

### 7.4 競合

- 管理データの保存時は、読み込み時のリビジョンと保存直前のファイル内容を比較して競合を検知する。
- 競合した場合は、編集中のモーダルだけでなく、管理画面にあるユーザー、組織、コメント対象、カレンダーの全下書きを破棄する。
- 競合発生後に自動再読み込みはしない。競合していた内容を確認せず上書きすることを避けるため、利用者自身に再読み込みしてもらう。
- 競合トーストは自動で閉じない。
- 表示文言: `別ユーザーがデータ変更していたため、更新を破棄しました。「最新データを再読み込み」を押してください。`

### 7.5 削除

ユーザー、課、係の削除前には確認ダイアログを表示する。課・係の連動削除では、件数や具体的な係名・ユーザー名を列挙せず、連動して何が変わるかを簡潔に説明する。ユーザー削除では削除対象の社員番号を示す。

課の削除確認:

`この課を削除すると、配下の係も削除され、所属ユーザーは「所属登録なし」になります。関連するコメント対象設定も解除されます。`

係の削除確認:

`この係を削除すると、所属ユーザーは「所属登録なし」になります。関連するコメント対象設定も解除されます。`

削除時のデータ処理は次のとおり。

- 課を削除した場合、その配下の係もすべて削除する。
- 削除対象の課・係に所属していたユーザーは削除せず、`所属登録なし` に変更する。
- 削除対象組織に紐づくコメント担当設定を解除する。
- カスタムコメント対象に含まれる該当ユーザーを対象一覧から除外し、対象が空になった場合はコメント対象を `なし` にする。
- ユーザー削除時も、そのユーザーがコメント担当・コメント対象になっている設定を整理する。
- 保存に失敗した場合は、削除前の一覧・選択・展開状態に戻す。

削除成功時のトーストは次のとおり。

- `ユーザーを削除しました。`
- `課を削除しました。`
- `係を削除しました。`

削除失敗時は、ユーザー削除では `ユーザーを削除できませんでした。`、課・係の編集モーダルでは `削除できませんでした。もう一度削除してください。` を表示する。

### 7.6 並び替え

- 課・係またはユーザーの並び替え後は自動保存する。
- 保存成功時はトーストを表示しない。
- 保存失敗時は並び替え前の順序へ戻す。
- 失敗文言: `表示順を保存できませんでした。もう一度並び替えてください。`

### 7.7 カレンダー設定

- カレンダー設定の保存成功: `カレンダー設定を保存しました。`
- 保存失敗: `カレンダー設定を保存できませんでした。もう一度保存してください。`
- 競合時は他の管理データと同じ競合処理を行い、管理画面の全下書きを破棄して再読み込みを促す。
- カレンダーの年度下限は2026年度とし、可変値は `app/calendar_policy.py` に集約する。

## 8. 実装上の参照先

| 内容 | 主な実装ファイル |
| --- | --- |
| 共通トースト、画面遷移、未保存確認 | `web/app.js` |
| 日報の読み込み・保存・日付範囲 | `web/reports.js` |
| 設定の読み込み・保存・入力検証 | `web/settings.js` |
| 管理画面、モーダル、競合、削除、並び替え | `web/admin.js` |
| 読み込み・保存API、キャッシュ同期 | `app/api.py` |
| 管理データのリビジョン検査とCSV保存 | `app/repository.py` |
| カレンダー年度下限と開始日デフォルト | `app/calendar_policy.py` |

CSS・JavaScriptの読み込みURLにはキャッシュ更新用のバージョンを付与する。現在のバージョンは `20260830-18` である。
